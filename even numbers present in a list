import sys
nums=[1,3,1,5,7,9]
ans=[]
for i in nums:
    if i%2==0:
        ans.append(i)
    #print(ans)
if len(ans)==0:
    print("not found")
    sys.exit(0)
min=ans[0]
for i in ans:
    if i<min:
        min=i
print(min)
